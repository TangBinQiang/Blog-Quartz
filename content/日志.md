### 2026/1/20

> [!Info]
> 问题：文章目录过多导致目录超出底部
> 现状：已解决

```diff
.toc {
  display: flex;
  flex-direction: column;
  overflow-y: hidden;
  min-height: 1.4rem;
- flex: 0 0.5 auto;
+ flex: 0 1 auto;
  &:has(button.toc-header.collapsed) {
    flex: 0 1 1.4rem;
  }
}
```

``` CSS
.toc {
  display: flex;
  flex-direction: column;
  overflow-y: hidden;
  min-height: 1.4rem;
  flex: 0 1 auto;
  &:has(button.toc-header.collapsed) {
    flex: 0 1 1.4rem;
  }
}
```

```css title="styles/toc.css"
.toc {
  display: flex;
  ...
}
```

```css :path/to/file.css
.toc {
  display: flex;
  ...
}
```

```css
/* src/styles/toc.css */
.toc {
  display: flex;
  ...
}
```

**`src/styles/toc.css`**
```css
.toc {
  display: flex;
  ...
}
```